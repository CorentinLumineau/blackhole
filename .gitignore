node_modules/
.bc-campaign/
.DS_Store
dist/
build/
*.log

# Ignore ephemeral handoff dirs and workspace plugin symlinks under .agents/
.agents/*
# Re-include compiled Gemini workspace tree only
!.agents/build/

# Gemini/Antigravity plugin manifest (track compiled plugin.json only)
.gemini-plugin/*
!.gemini-plugin/plugin.json

# Codex CLI build outputs (committed for zero-build marketplace install — #31)
# .codex-plugin/, codex-skills/, codex-agents/, codex-marketplace.json are tracked
