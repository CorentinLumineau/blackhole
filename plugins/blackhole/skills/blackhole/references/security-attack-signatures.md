# Security attack signatures (diff-scoped)

These rows are **search aids for changed lines only** — apply patterns from this file when
the matching construct appears in the PR diff. A pattern hit is never sufficient for a finding:
every security issue still requires a concrete attack scenario per `V-SEC-06` (who/what/result).
Signatures narrow where to look; they do not bypass exploitability validation.

## Injection

| CWE | Search pattern | Exploitable when |
|-----|----------------|------------------|
| CWE-89 | `rg -n -- '(SELECT|INSERT|UPDATE|DELETE).*\$\{|\.query\([^)]*\+|\.query\(\`[^\`]*\$\{'` on changed SQL/ORM call sites | User-controlled input reaches the query string without parameterization or equivalent binding |
| CWE-943 | `rg -n -- '\$where|\$regex|\$gt|\$ne|findOne\(\s*\{|find\(\s*\{[^}]*req\.|aggregate\(\s*\[` on changed NoSQL accessors | Request body/query keys or operator objects are merged into a filter without schema allowlisting |
| CWE-78 | `rg -n -- 'exec\(|execSync\(|spawn\([^,]*shell|system\(|popen\(|subprocess\.(call|run|Popen)\([^)]*shell=True'` on changed lines | Externally influenced data is interpolated into a shell command or passed to a shell wrapper |
| CWE-1336 | `rg -n -- 'render_template_string|Template\(|\.compile\([^)]*\+|eval\(|new Function\(|Handlebars\.compile\([^)]*req'` on changed template/eval paths | Attacker-controlled template source or expression is compiled/rendered server-side |

## Broken access control / IDOR

| CWE | Search pattern | Exploitable when |
|-----|----------------|------------------|
| CWE-639 | `rg -n -- 'findById\(|findOne\(\s*\{[^}]*id:|get\([^)]*:(id|uuid|slug)|params\.(id|userId)|req\.params'` on changed handlers lacking an ownership predicate in the same hunk | Authenticated caller can substitute another subject's identifier and read/write that resource |
| CWE-285 | `rg -n -- '@(Get|Post|Put|Patch|Delete)\(|router\.(get|post|put|patch|delete)\(|app\.(get|post|put|patch|delete)\('` on new/changed routes without a nearby authz guard (`authorize`, `requireRole`, `can(`, policy check) | Endpoint performs a sensitive action with authentication only (or none) and no role/permission enforcement |

## Permissive CORS

| CWE | Search pattern | Exploitable when |
|-----|----------------|------------------|
| CWE-942 | `rg -n -- 'Access-Control-Allow-Origin.*\*|cors\(\s*\{[^}]*origin:\s*true|origin:\s*[\"'\'']\*[\"'\'']|allowedOrigins:\s*\[\s*[\"'\'']\*[\"'\'']'` on changed CORS/middleware config | Wildcard or reflected origin is combined with credentialed cross-origin access to sensitive endpoints |

## Weak cryptography

| CWE | Search pattern | Exploitable when |
|-----|----------------|------------------|
| CWE-328 | `rg -n -- 'md5\(|sha1\(|MD5|SHA-1|createHash\([\"'\'']md5|createHash\([\"'\'']sha1|bcrypt\.hashSync\([^,]+,\s*[0-5]\)'` on changed password/token digest code | Secret or password material is hashed with a fast/legacy digest or trivial cost factor for storage or verification |
| CWE-256 | `rg -n -- 'password\s*=\s*[\"'\''][^\"'\'']+[\"'\'']|PASSWORD\s*=\s*[\"'\'']|apiKey\s*=\s*[\"'\''][^\"'\'']+[\"'\'']'` on changed credential fields | Cleartext password/API secret is persisted or compared without a one-way transform |

## Unsafe deserialization

| CWE | Search pattern | Exploitable when |
|-----|----------------|------------------|
| CWE-502 | `rg -n -- 'pickle\.loads|yaml\.load\(|unserialize\(|readObject\(|JSON\.parse\([^)]*req\.|deserialize\(|marshal\.loads'` on changed parsers | Untrusted bytes/strings are deserialized into executable object graphs or types with gadget chains |

## SSRF

| CWE | Search pattern | Exploitable when |
|-----|----------------|------------------|
| CWE-918 | `rg -n -- 'fetch\([^)]*req\.|axios\.(get|post)\([^)]*req\.|http\.get\([^)]*req\.|urllib\.request\.urlopen\([^)]*req\.|new URL\([^)]*req\.'` on changed outbound HTTP clients | Attacker supplies URL/host and the server performs the request without blocklisting metadata IPs, link-local, or internal ranges |
<!-- GENERATED by scripts/build.ts from src/references/security-attack-signatures.md — do not hand-edit -->
