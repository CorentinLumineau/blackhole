---
type: reference
summary: "Running decision log of Hard Choice / Bugfix / Refactoring decision records"
status: current
review_trigger: "on file change"
created: 2026-07-20
last_updated: 2026-09-06
related:
  - documentation/decisions/ADR-012-shared-artifact-substrate.md
---

# Decision Log

Durable, greppable record of implementation decisions — Root-Cause Decision Records,
Refactoring Verification Decision Records, Reuse Check entries, and Improvement Records —
banked by the orchestrator from `decision_records[]` (ADR-012 E4). **Append-only. Written
solely by the orchestrator**, serially, post-barrier — see `src/agents/orchestrator.md` §
Decision Record Append. No worker writes this file directly.

## Rotation

When this table exceeds 500 rows, the orchestrator moves the oldest rows to
`documentation/reference/_archive/decision-log-{first-issue}-{last-issue}.md`, mirroring the
`findings-ledger.json` archive convention (`src/references/blackhole-state.md`). This file
itself is never deleted, only trimmed.

## Known Baseline (decision_log_silent_prs)

`decision_log_silent_prs` (`scripts/doc-health-signal.ts`) counts merged PRs whose number never
appears in the Records table below. As of issue #767 the count is ~168/173 merged issues — this
is expected steady-state, not a live defect. Two structural, non-defect causes dominate it:

- **Pre-mechanism history (structurally silent, permanent)**: this file and the orchestrator's
  append step did not exist before commit `ca1f6a1d` (2026-07-20, issue #421/#422). Every merged
  issue that landed before that date has no mechanism it could have hit — it is unrecoverable
  history, not a gap to close.
- **A closed six-week implementation gap (2026-07-20 → 2026-09-02)**: the append mechanism was
  documented (`src/agents/orchestrator.md` § Decision Record Append) from `ca1f6a1d` onward, but
  the invocable script, `scripts/decision-log-append.ts`, was not written until commit
  `2fe253e9` (2026-09-02, issue #750). Any `decision_records[]` a worker returned in that window
  had no automated path into this log. Issue #749 / PR #770 (commit `221e7030`) already recovered
  the 17 rows that survived in an uncommitted working-tree file from that window; anything else
  from that window that was never persisted to disk is permanently unrecoverable — there is
  nothing further to retroactively "fix" for it.

Since `2fe253e9` landed the mechanism has run again — PR #810's rows landed via commit
`2e51e674` — confirming the wiring works going forward. The current count is therefore the
expected floor from this point on; it will never shrink (history is unrecoverable). The only
thing worth watching is **growth past this floor after 2026-09-02**: a future merged PR whose
implementer worker returned a non-empty `decision_records[]` that still fails to reach this log
would indicate the wiring broke again. This does not change the signal's existing advisory
framing (`V-DOCHEALTH-03`: `doc_debt` stays advisory, no ledger append, no phase gate) — it only
records why the number is what it is, so the question is not re-litigated on every turn.

## Records

| PR/Issue | Kind | Touch Paths | Decision | Why |
|---|---|---|---|---|
| PR #428 / #421 | reuse | src/references/hunt/ux-coherence.md | Reused parity.md structure + kaizen-parity-kind.test.ts test shape for the 8th hunt kind | One-file-per-kind is the established extracted pattern; rule-of-three N/A |
| PR #428 / #421 | improvement | src/references/config-template.md | No improvement needed beyond plan's registration edits | Touched lines already clean; formatting convention preserved |
| PR #430 / #422 | reuse | scripts/lib/worker-json/validators/planner.ts | Reused validateBrainstormChild local-helper shape for validateRulingConflictEntry/validateRulingConflicts instead of extracting a shared combinator | 5 bespoke occurrences now exist (past rule-of-three) but extraction is out of #422 scope — filed as #431 |
| PR #430 / #422 | improvement | src/references/clarify-gates.md | Replaced stale forward-reference ("#422 owns...not that classifier") with live pointer to the new fork + coordinator disposition wiring | Prose promised future work this PR delivers; leaving it would be a stale claim |
| 732 | approach | `documentation/architecture.md` | Used the exact lowercase phrase "maintainer-local, not an install path (ADR-009)" verbatim in the `.claude/` row | The plan AC verifies with a case-sensitive grep; matching exactly avoids a false negative in the plan's own verification step |
| 733 | approach | `scripts/lib/build/facts.ts`, `scripts/verify.ts`, `scripts/lib/plan-touch-path-ssot-pairs.ts`, +5 | Fully retire `EXPECTED_CHECK_COUNT`, `warnOnCheckCountMismatch` and the `TOUCH_PATH_SSOT_PAIRS` new-check-module trigger rather than keep the manual-bump pattern | The constant compared `verify.ts`'s own `results.length` against a hand-bumped literal — one source computing both sides of its comparison, never fitting the § facts SSOT convention; with no companion scan to preserve, deletion removes the recurring hot-file contention instead of tolerating it |
| 734 | root-cause | `templates/hooks/pretooluse/utils/hook-event-log.js`, `scripts/hooks-validate-file.test.ts` | Reuse the file's existing realpath-based `resolveExistingAncestor` to build `BARE_TEMP_DIRS` and resolve the incoming value, instead of `path.resolve` or a darwin-only branch | `path.resolve` does not follow symlinks, so a bare temp root reached through a symlinked ancestor evaded the guard; `resolveExistingAncestor` is already used for every other containment comparison in this file (V-INT-02), and the fix generalizes beyond the macOS instance |
| 734 | approach | `documentation/INDEX.md` | Append-merge of the rebase conflict: kept both sides' rows verbatim, ordered alphabetically by path within each block | `INDEX.md` is an append-only table where each landed doc owns one row; both hunks were positional, not semantic edits to a shared row, so preserving every row was the whole resolution — verified 39 rows on main → 41 on the branch, 2 additions, 0 deletions |
| 735 | root-cause | `templates/companion-files/README.md` | Corrected the README's stale claim that `journeys.md` targets the repo root, to `documentation/reference/journeys.md` | `doc-health.check.ts` only walks `documentation/`, so a repo-root file could never trip `V-DOCHEALTH-02`, yet the issue reports exactly that failure; the template's own lifecycle frontmatter confirms the `documentation/reference/` target |
| 735 | refactor | `scripts/lib/check-common.ts`, `scripts/checks/doc-health.check.ts` | Hoisted `RootIndexRow`/`appendIndexRowIfAbsent` into `check-common.ts`, re-exported from `doc-health.check.ts` | `companion-file-sync.ts` in `lib/` needed the same idempotent-append primitive but `lib/` cannot import a `*.check.ts` module per `check-common.ts`'s documented layering; the shim meant zero edits to the existing test |
| 739 | approach | `src/references/review-core.md`, `src/agents/reviewer.md` | Replaced the flat 5-item checklist claim with a per-mode table and had §13/§24 cite it by name instead of restating their input contract | Restating the contract at three sites lets them drift independently (V-DOC-05); one canonical table matches the repo's primary-enforcement-site discipline |
| 740 | approach | `scripts/checks/config-registration.check.ts`, `src/references/config-template.md` | Teach `parseConfigTemplateKeys` to honour a `(sub-keys: …)` marker on a documented parent row, rather than adding one Field row per leaf | #723 will attach a `resolution:` sentence to each nested block's single canonical row; 8 per-leaf rows would leave that target ambiguous and would not close the general defect class — one parser extension closes it permanently |
| 740 | approach | `documentation/INDEX.md` | Append-merge of the rebase conflict, inserting the branch's plans row into the plans section rather than at the file tail | Preserves both entries without duplication and matches the recurring pattern filed as #743 |
| 740 | approach | `documentation/reviews/review-fix-config-registration-…md` | Corrected the generated verdict from CHANGES REQUESTED to LGTM with the WARN disclosed as deferred, instead of patching the generator | True merge-readiness is LGTM (0 blockers, one WARN deferred to #736); fixing the `selectReviewFindings` defect is #737's job, not a mechanical promotion's |
| 742 | approach | `scripts/build.test.ts` | Placed the new skills-target test inside the `applyPlatformConditionals` describe block rather than at the plan's cited line numbers | The cited lines pointed at the `compileContent` block in the current file; `applyPlatformConditionals` is the function under test and the plan's primary instruction named it, so the citation was stale rather than the instruction wrong |
| 742 | approach | `documentation/INDEX.md` | Resolved the rebase conflict as an append-merge, keeping `main`'s row from #735 and this branch's plan row | The now-familiar mechanical pattern (#743); no semantic conflict existed, so no alternative was weighed — arithmetic verified 45→46 rows, 1 addition, 0 deletions, 0 duplicates |
| 742 | approach | `documentation/INDEX.md` | Second rebase after #740 merged and re-conflicted the file; append-merge keeping both sides' rows again | Sixth instance of the recurring collision (#743) and the first repeat on one PR — evidence the cost is one rebase per *intervening merge*, not per PR |
| 744 | reuse | `scripts/lib/build/facts.ts`, `scripts/verify.content-gates.test.ts` | Seeded three budget rows via the existing measured × 1.2 convention, re-measuring at the PR's own base rather than copying the plan's table | The plan was written 7 merges earlier; re-measurement confirmed zero delta, and reusing the established row shape avoided a differently-shaped variant (V-INT-01/V-INT-03) |
| 744 | reuse | `documentation/reviews/review-chore-content-gates-…md` | Added the `review-aggregate.ts` gate row alongside the generator's single ledger row | Every merged artifact (#706, #709, #713, #714) carries both rows; shipping the generator's bare output would have made this a fifth format variant at an established touchpoint |
| 751 | root-cause | `scripts/verify.model-routing-effort.test.ts` | #742 correctly replaced the "Unverified" note with a citation; the assertion in another file was outside its Touch-Paths and went stale | The prose is the intended end state, so the test was wrong, not the doc — a revert would have been the symptom fix |
| 751 | approach | `scripts/verify.model-routing-effort.test.ts` | Cross-file guard (prose names the cited test; the cited test still exists there) over a `/verified/i` word swap | A word swap passes on any prose containing "Verified", including a lie or a citation to a deleted test; the guard fails if either side drifts — proven by breaking each side in turn |
| 786 | root-cause | templates/hooks/pretooluse/utils/worktree-removal-guard.js | Added isPathQualifiedGitWordStart, ORed into findGitWordIndices, rather than widening isCommandWordStart's predecessor class to include '/' | isCommandWordStart's [\s;&\|(\n] class excluded '/', so a path-qualified git word was discarded and evaluateWorktreeRemoval returned null — fail-open. Widening the class would newly admit the --git-dir=/x/git fragment; the new predicate walks back to the real token boundary with an '=' crossing check instead |
| 786 | reuse | templates/hooks/pretooluse/utils/worktree-removal-guard.js | No reusable predicate found repo-wide; isCommandWordStart left untouched and extended by OR rather than modified | First occurrence of a path-qualified command-word predicate in the tree; extending by OR keeps the existing predicate's contract intact for its other caller |
| 786 | improvement | templates/hooks/pretooluse/utils/worktree-removal-guard.js | Restated findGitWordIndices' docstring to cover path-qualified tokens | Scout Check within the diff boundary; the existing docstring described only the shell-separator case and would have read as contradicting the new predicate |
| 810 | root-cause | scripts/lib/carry-target-allowlist.ts, scripts/lib/carry-staged-artifacts.ts | Chose an explicit target_path allowlist (documentation/**, root ARCHITECTURE.md) over patching only the ENOTDIR throw | The narrow fix would have left the arbitrary-code-execution vector (package.json, .github/workflows/*.yml, .git/hooks/*) open; the allowlist bounds the write surface to what artifact-contract.md's route table ever legitimately produces |
| 810 | root-cause | scripts/lib/carry-staged-artifacts.ts | Normalized target_path before the allowlist comparison, matching containment's existing normalize-before-compare against repoRoot | Containment normalized the path before comparing against repoRoot; the allowlist did not normalize before comparing against documentation/**. Two gates, two different representations of the same path — that mismatch, not either gate individually, was the vulnerability |
| 810 | reuse | scripts/lib/carry-target-allowlist.ts | New scripts/lib/carry-target-allowlist.ts modeled on ops-touch-paths.ts's named-glob-array + boolean-predicate shape | Reuse check found no existing carry-target-allowlist primitive (repo-wide grep returned 5 unrelated hits) — first occurrence of this concern |
| 810 | improvement | scripts/lib/carry-staged-artifacts.test.ts | Flipped the stale /etc/passwd 'keeps carrying' assertion whose comment explicitly warned against flipping it, and rewrote the comment to record that AC1's allowlist supersedes it | Scout Check within the diff boundary; containment still holds, it was simply never sufficient on its own to make a path safe to write |
| 848 | reuse | src/references/phase-review.md | Reused the existing citation idiom `review-core.md` § Reviewer prompt requirements (2 prior bespoke occurrences in reviewer.md) | Below the rule-of-three threshold, so no extraction was owed; matching the local idiom avoids a second citation variant |
| 848 | improvement | src/references/phase-review.md | Dropped the one trailing period in the retained 'Route check' bullet for list consistency | Scout Check within the diff boundary; the other four bullets carry no terminal period |
| 849 | reuse | src/agents/implementer.md | No utility introduced; retargeted citations follow the file's existing backtick-filename plus § heading idiom | Pure citation retarget — matching the one established local citation form avoids a second variant of a solved concern (V-INT-04) |
| 849 | improvement | src/agents/implementer.md | Also retargeted the un-cited stale pointer at line 463 ('No new worker-schemas.md return field') to implementer-schemas.md | Same defect class as the seven cited lines, inside code the diff already touches; leaving it would send readers to the wrong file |
| 850 | reuse | src/references/orchestrator-delegation.md | None found — first occurrence of contended-path exclusion guidance repo-wide; new section follows the existing `### <Topic>` heading + cite-by-§ idiom | Existence search surfaced only queue-dag.md's issue-level conflict filter, a different concern, which the new section cites rather than restates |
| 850 | improvement | src/references/orchestrator-delegation.md | Field 3's existing sentence extended with one forward pointer rather than rewritten | The touched line was otherwise already clean; no further improvement within the diff boundary |
| 851 | root-cause | src/SKILL.md, scripts/checks/cwd-pin-guard.check.ts | Two defects: the SKILL.md invocation omitted the --cwd pin, and the guard's sweep scope never read root src/SKILL.md; fixed both via an explicit SWEEP_FILES unioned by an exported sweepTargets() | Pinning the call site alone leaves the blind spot; a recursive walk silently pulls in the deliberately excluded hunt/*.md; an explicit, exported scope is assertable rather than inferred from the verdict |
| 851 | reuse | scripts/checks/cwd-pin-guard.check.ts | Reused findMissingCwdPin as the single unpinned-invocation detector, plus listFiles/read from check-common and check-utils | The scope extension feeds the existing matcher new targets rather than adding a second matcher |
| 851 | improvement | scripts/checks/cwd-pin-guard.check.ts | No improvement needed; read() throwing on an absent SWEEP_FILES entry was kept deliberately | A missing sweep target must fail loudly rather than degrade to a silent pass — the property this fix restores |
| 852 | root-cause | src/references/implementer-schemas.md, src/references/worker-schemas.md, scripts/lib/worker-json/validators/implementer.ts | State pr_number once as the canonical top-level spelling, have the reviewer contract cite it, and make the implementer validator reject a top-level `pr` by name — no alias | The reported reviewer-vs-implementer divergence does not exist; the real cause is one contract carrying both spellings at different nesting levels with no rule saying which applies where, and a diagnostic that never names the field actually supplied |
| 852 | reuse | scripts/lib/worker-json/validators/implementer.ts | Reused the existing errors.push('<field>: …') diagnostic idiom instead of extracting a helper | Every validator reports field-level problems this way; a validateCanonicalPrField helper would be a single-consumer abstraction (V-YAGNI-03) |
| 852 | improvement | scripts/lib/worker-json/validators/implementer.ts | No improvement needed — code already clean | Reconciling the mixed `'x' in data` guards would change acceptance behaviour for explicit-undefined payloads, outside this diff's scope (V-SCOPE-01) |
| 853 | root-cause | templates/hooks/pretooluse/utils/hook-event-log.js | allWorktreeRoots() used the validated scratchpad_dir only as a filter predicate, never as a member of the returned root set; admit it (and the BLACKHOLE_SCRATCHPAD_DIR leg) as a root gated on isExistingDirectory | Widening cwd-trust to cwd's parent is unbounded, an env opt-in cannot fix the documented config path, and admitting scratchpad_dir unconditionally lets a never-created value collapse onto its parent via resolveExistingAncestor and trust every sibling there |
| 853 | reuse | templates/hooks/pretooluse/utils/hook-event-log.js | None found — first occurrence of an 'existing directory' trust predicate; reused hook-event-log.js's local try/catch arrow-predicate idiom, one predicate serving both scratchpad legs | The six statSync/isDirectory hits repo-wide are directory-walker recursion guards, a different concern, and the standalone hooks JS cannot import scripts/lib/*.ts |
| 853 | improvement | templates/hooks/pretooluse/utils/hook-event-log.js | Refreshed allWorktreeRoots's docstring to name the scratchpad root and its existence guard; kept the seemingly redundant isUnderRoot(root, scratchpadDir) filter clause | readAssignedWorktreeRoot matches BLACKHOLE_ASSIGNED_WORKTREE against exact members of this list, so dropping nested worktrees would break assigned-worktree narrowing |
| 854 | reuse | scripts/lib/worker-json/enum-source.ts, scripts/validate-worker-json.ts | No existing utility loads a role validator from a caller-named tree; wrote resolveValidateWorker as the first occurrence using scripts/verify.ts's await import(<absolute path>) idiom; kept the 22-site `error instanceof Error` idiom rather than extracting a helper | A new loader wrapper would be a second variant of a solved concern, and extracting the error-message helper would touch ~15 files outside Touch-Paths (V-SCOPE-02) — filed as a finding instead |
| 854 | improvement | scripts/validate-worker-json.ts, scripts/lib/worker-json/enum-source.ts | No improvement needed — the touched functions are single-purpose and follow the file's conventions | The only repeated idiom inside the diff boundary is the repo-wide error-message pattern, deferred as out of scope rather than half-extracted in one file |
| 855 | reuse | scripts/lib/check-common.ts, scripts/checks/vcode-citation.check.ts | Reused parseVcodeTableRows for the table side; parseEmittedVcodeIds is the first occurrence of an emitted-id-literal scanner, placed in check-common.ts beside the parser it pairs with | Repo-wide search found only leg A and ground-truth.check.ts's narrower {code, site} extraction, neither scanning check-module sources; co-location keeps V-INT-02 and respects the 218-LOC check-file ceiling |
| 855 | improvement | scripts/verify.vcode-citation.test.ts | Added a shared readFixtureText helper reading from fixtures/vcode-citation/, replacing inline fixture strings; reverted a codeAppearsIn tightening that surfaced a real V-CITE-02 near-miss needing an out-of-Touch-Paths reviewer.md edit | Aligns the test with the repo's fixtures/ convention; the tightening was reported as a finding rather than widening the PR |
| 856 | reuse | scripts/checks/adr-shape.check.ts, src/references/blackhole-vcodes.md, src/references/doc-governance.md | None found — first occurrence of 'advisory by decision, not by omission' governance phrasing; followed queue-coherence.check.ts's existing advisory-header idiom | Repo-wide search returned zero hits and the neighbourhood surfaced exactly one established advisory-header idiom; no new abstraction introduced |
| 856 | improvement | scripts/checks/adr-shape.check.ts | No improvement needed — code already clean | The only change is an appended header paragraph; naming, error handling and imports required no change |
| 857 | reuse | scripts/merge-base-guard.ts, scripts/lib/merge-gate/merge-base.ts | None found — first occurrence of PR-base assertion / post-merge landing verification; built as a lib/CLI pair mirroring merge-gate/review-artifact.ts + check-review-artifact.ts | Repo-wide search for baseRefName returned zero hits; the merge-gate lib/CLI pair is the established local idiom, so the new code follows its usage()/parseArgs shape and exit-code contract rather than inventing a third variant |
| 857 | improvement | src/references/phase-loop.md, src/references/queue-dag.md | Step 4.7 and the merged_into row each state why the field is not written by step 4, instead of editing step 4's adjacent merged_by sentence | Keeps the touched text self-explanatory without editing untouched prose a sibling worker (#794) is concurrently in (V-SCOPE-01) |
| 858 | reuse | src/references/blackhole-vcodes.md | None found — first occurrence of settled V-DOC-06 boundary text; every rewrite follows the local idiom (comment states the invariant and cites the defining symbol or ADR, never the issue number) | The diff introduces no utility, helper or abstraction, only comment text |
| 858 | improvement | scripts/migrate-ledger-schema.ts | Replaced the header's hardcoded live-data count ('27 live rows') with the invariant stated by row shape | A count can only go stale and would have become a V-DOCFACT-01 claim as soon as the ledger grew |
| 859 | reuse | documentation/decisions/ADR-038-unknown-build-tree-dispositions.md, scripts/checks/tree-registry.check.ts | Reused ADR-007 R5′ tracked⇒built-by-default and paths.ts COMMITTED_TARGET_TREES / V-TREE-01; ADR-038 disposes of rows inside the existing registry; formatTreeRegistryDetail follows the file's exported-pure-helper idiom | Only 2 prior build-tree disposition ADRs exist (below rule-of-three); no new registry, predicate or mocking layer introduced |
| 859 | improvement | documentation/architecture.md | Corrected § Build & verify's stale `bun run build --gemini` advice to the tracked⇒built-by-default behaviour | --gemini has been a parsed-but-ignored deprecation alias since ADR-007 T2; the file was already in the diff |
| 860 | reuse | scripts/stack-repair.ts, scripts/lib/campaign-status/types.ts | None found — first occurrence of stacked-PR tip capture and --onto repair; reused validateStateWrite, runGh (sole gh spawn site, V-FORGE-01), makeTempDir and triage-deferred-findings.ts's dry-run-by-default CLI shape | Repo-wide grep for rebase --onto / parent_tip / stacked_on returned zero prior implementations; the four utilities already cover state writes, forge calls, temp dirs and the CLI idiom |
| 860 | improvement | scripts/lib/campaign-status/types.ts | Added the one-line queue-dag.md § Field rules citation to QueueIssue, matching the sibling Route type | Both types now name where their field semantics are frozen |
| 861 | reuse | templates/hooks/pretooluse/utils/worktree-removal-guard.js | Did not reuse allWorktreeRoots for worktree registration; wrote isRegisteredLinkedWorktree; the git and rm spellings share one verdict body (evaluateResolvedWorktree) over the same three check functions | allWorktreeRoots is by its own docstring a narrowed containment-trust list, not every registered worktree — reusing it would let a filtered-out worktree be silently removable by rm -rf (under-detection) |
| 861 | improvement | templates/hooks/pretooluse/utils/worktree-removal-guard.js | Replaced the negated-continue executable dispatch in findRemovalInvocations with a named basename local and an explicit branch chain | The old form could not express a second executable; the rm arm now reads as a peer of the git arm |
| 862 | refactor | src/agents/implementer.md, src/references/gates/ | Shallow restructuring, verbatim move: gate bodies unchanged except heading level; module order and separators reproduce the original document; prose scans that read the shell now read the composed document (readComposedAgentDoc) or sweep BUILD_INPUT_ONLY_DIRS | Mirroring gates/ into compiled trees would ship every module twice per consumer tree, forbidden by ADR-034 and V-INCLUDE-01; leaving marker-presence checks reading the shell would make them pass vacuously |
| 862 | reuse | scripts/lib/build/content.ts, scripts/lib/check-common.ts, scripts/checks/ground-truth.check.ts | Reused expandIncludes, findMissingGateMarkers, findRowCountMismatch and listFiles — no new parser, comparator or expansion path | Repo-wide search found exactly one implementation of each concern; rule-of-three did not fire |
| 862 | improvement | scripts/checks/staging-schema.check.ts | Folded collectProducerLiterals' three near-identical extract-then-tag lines into one flatMap over the doc list | V-DRY-02 shape inside a function the diff already had to touch for the composed read |
| 867 | reuse | scripts/checks/read-json-file.check.ts, scripts/verify.read-json-file.test.ts | Reused scripts/checks/jq-empty-guard.check.ts's drift-prevention check shape and vocabulary.check.ts:128 (scanScriptsTs) .ts-exclude-tests pattern for the new V-JSONREAD-01 detector | No existing bare-JSON.parse detector was found repo-wide (first occurrence of this concern); the established 'pin the class' check shape and scripts-tree scan idiom already existed and fit exactly, so extending them beat inventing a third pattern (V-INT-03) |
| 867 | improvement | scripts/checks/read-json-file.check.ts | Reworded the module doc comment to describe the bypass shape in prose instead of spelling out the literal matched token sequence | The first draft's comment literally contained the matched code pattern and made the check flag itself (a self-referential false positive caught by the red-before-green run); the fix also documents why the comment is phrased that way, preventing recurrence |
| 867 | approach | scripts/checks/claude-native-settings.check.ts, scripts/checks/codex-build.check.ts, scripts/checks/config-registration.check.ts, scripts/checks/deferred-reconciliation.check.ts, scripts/checks/hooks.check.ts, scripts/checks/ledger-schema.check.ts, scripts/checks/playbook.check.ts, scripts/checks/queue-coherence.check.ts, scripts/checks/reformulation-surface.check.ts, scripts/lib/hook-event-triage.ts, scripts/lib/test-fixtures.ts, scripts/stack-repair.ts, scripts/triage-deferred-findings.ts | Migrated all 16 bare JSON.parse(fs.readFileSync(...)) call sites onto scripts/lib/fs.ts's readJsonFile(path, path) as pure substitutions, preserving every surrounding try/catch exactly as-is | Easy path (leave the 6 LOW-severity static-manifest sites unmigrated since their observable behavior change is smallest) vs hard path (migrate all 16 uniformly): chose the hard path per the plan's severity classification, which found no site with a genuinely different failure mode — a future readJsonFile hardening (e.g. a zero-byte guard) reaches every site for free only if none are left as bypasses |
| 868 | reuse | scripts/lib/forge-adapter/gitlab.ts | Reuse Check: reusing runGlabJson (scripts/lib/forge-adapter/glab-cli.ts:21) | The prChecks fix issues its two new GitLab REST calls through the existing runGlabJson wrapper, exactly like every other adapter method in gitlab.ts and mirroring runGhApiJson's generic-passthrough pattern in cli.ts (Codebase Conventions row 6) — no new CLI wrapper or helper was introduced; the routing check confirms no second spawn site was created. |
| 868 | improvement | scripts/lib/forge-adapter/gitlab.ts | No improvement needed — code already clean | The prChecks method body is the only touched production code; it computes encodeURIComponent(this.repo) once and reuses it for both API calls, follows the existing status/conclusion enum-mapping style already used in this file and in github.ts:normalizeCheck, and carries a comment documenting both the #864 no-swallow convention and the #868 shape-bug rationale. No dead code, unclear naming, or missing error handling was found within the diff boundary. |
| 871 | root-cause | scripts/lib/check-common.ts, scripts/lib/check-common.test.ts | Rewrote appendIndexRowIfAbsent to operate on raw row-line text and a canonicalized path key instead of parseIndexTableRows/renderIndexRowLine round-tripping every row. | Root cause: the function reparsed and re-rendered every row via a naive split('\|') that does not respect GFM \\| escapes (Defect 1, drops/shifts cells on re-render) and sorted on raw cell text where [ (0x5B) sorts ahead of lowercase paths (0x61+), clustering markdown-link-wrapped rows ahead of bare-path rows regardless of true order (Defect 2). Fixing only the sort comparator or only the escape-handling would leave the other corruption mode live; both defects share the same root cause (full-row reconstruction of untouched rows) so one rewrite closes both. |
| 871 | reuse | scripts/lib/check-common.ts | Reused decision-log-append.ts's insertRecordRowsSorted technique (operate on raw row-line text, never reconstruct from parsed fields) for appendIndexRowIfAbsent's rebuild; did not reuse its recordSortKey (numeric first-digit-run) since this schema's sort key is a path string with two rendering conventions in the same file. | Existence search found exactly one prior occurrence of the raw-line-preserving rebuild technique (decision-log-append.ts's insertRecordRowsSorted, issue #887) — reusing it avoids a second, divergent implementation of the same technique (V-INT-02). No existing implementation of markdown-link-path canonicalization was found repo-wide (first occurrence), so canonicalIndexPath is new, judged rather than assumed per the plan's explicit instruction. |
| 871 | improvement | scripts/lib/check-common.ts | No improvement needed beyond the fix itself — code already clean. | The rewritten appendIndexRowIfAbsent and its two new private helpers (canonicalIndexPath, rowPathFromRawLine) are each single-purpose, documented at their definition site, and covered by new unit tests; no dead code, stale comment, or naming issue was found in the touched region beyond what the fix itself already addresses. |
| 878 | root-cause | scripts/checks/cwd-pin-guard.check.ts, scripts/lib/companion-file-sync.ts | Widen sweepTargets() to the guard's own TARGET_SCRIPTS, prove it red against the live tree, then fix the Usage strings and extract a shared USAGE constant. | The root cause was a detection gap, not just a wrong string: cwd-pin-guard's sweep is entirely markdown (SWEEP_DIRS src/agents + src/references, SWEEP_FILES src/SKILL.md), so a Usage literal inside a .ts file was structurally unreachable and the same defect class stayed exploitable in the other three TARGET_SCRIPTS with zero coverage. Rejected the AC's literal scripts/**/*.ts sweep: eight other Usage-bearing scripts take no --repo-root distinct from their invocation cwd, so a blanket scan would manufacture false positives in a guard people currently trust. |
| 878 | reuse | scripts/checks/cwd-pin-guard.check.ts | reusing TARGET_SCRIPTS (cwd-pin-guard.check.ts:15-20) and findMissingCwdPin/invocationRegex unchanged | One hit; the sweep widening needed no new regex logic, only additional targets fed to the existing machinery. |
| 878 | improvement | scripts/lib/companion-file-sync.ts | collapsed two near-duplicated Usage literals into one module-level USAGE constant | Scout Check: two literals requiring synchronised edits is exactly the root-cause class this issue reports, so collapsing them to one definition is part of the fix rather than gold-plating. |
| 883 | root-cause | scripts/lib/worker-json/enum-source.test.ts, scripts/lib/worker-json/enum-source.ts | Root cause: LOCAL_CONST_NAME_BY_MEMBERS's collision-drop path (drops both names when two constants.ts arrays share a member list) had zero test coverage, and in particular no assertion that WAIVABLE_ENUMS members stay resolvable through it. Fix: export the map (one-line change, no logic touched) and add two tests — a mechanism regression on the two real pre-existing collisions (IMPLEMENTER_STATUSES/INVESTIGATOR_STATUSES, REVIEWER_STATUSES/HUNTER_STATUSES), and a load-bearing WAIVABLE_ENUMS-resolvability assertion, proven falsifiable by temporarily inducing a collision against COMPANION_REPAIR_VCODES and reverting. | The alternative (leave the collision-drop path untested) would let a future constants.ts addition silently make COMPANION_REPAIR_VCODES non-waivable with no test noticing, defeating PR #854's seven-iteration security hardening of the same file. |
| 883 | improvement | scripts/lib/worker-json/enum-source.test.ts | Scout Check: corrected the plan's illustrative AC #2 test snippet, which called `LOCAL_CONST_NAME_BY_MEMBERS.has(name)` where `name` is a WAIVABLE_ENUMS constant name. The map is keyed by joined member-list, valued by constant name (confirmed against the real waiveWidenedEnumErrors lookup at enum-source.ts:162), so a name is only ever a map VALUE, never a KEY: `.has(name)` is always false regardless of collision state. Rewrote the assertion to check membership in `Array.from(map.values())`, matching what the production lookup path actually resolves, and documented the distinction inline. | A test asserting `.has(name)` on the wrong axis of the map would always fail (or, if reversed to `.toBe(false)`, always pass regardless of a real regression) — either way it pins nothing. Fixing it in this session, rather than shipping a broken assertion the plan literally specified, is exactly the falsifiability guarantee this issue exists to add. |
| 883 | reuse | scripts/lib/worker-json/enum-source.test.ts | Reuse Check: reused the existing describe/test structure, fs/os/path fixture-tree helpers, and constants.ts import conventions already established in enum-source.test.ts (1-2 hits, same file) rather than introducing new test scaffolding. | No new utility or abstraction was needed — the two new tests are pure unit assertions against already-imported values, following the file's existing describe-block-per-concern convention. |
| 884 | reuse | src/references/blackhole-protocol.md | none found — first occurrence of documenting this residual, repo-wide | A wording addition to an existing limitations sentence, not a new utility; nothing pre-existing to extend. |
| 884 | improvement | src/references/blackhole-protocol.md | no improvement needed — code already clean | Scout Check on a prose-only diff; clause (1) now names the {} placeholder class alongside $VAR/glob/command-substitution, and clause (2)'s distinct never-recognized class is untouched (invariance greps hold at 1). |
| 885 | root-cause | src/references/queue-dag.md, scripts/lib/worker-json/predicates.ts, scripts/lib/worker-json/validators/router.ts | Pin sha256(title + \n + body) as canonical in queue-dag.md and add an isBodyHash 64-hex predicate replacing bare isString in router.ts, rather than patching issue 868's corrupted value. | A value-only patch leaves the same corruption class free to recur; the shape check makes it structurally impossible for a malformed digest to pass validation silently again. Confirmed the corrupted value passed today: 63 valid hex chars satisfy isString. |
| 885 | reuse | scripts/lib/worker-json/predicates.ts | none found — first occurrence of concern, repo-wide | No existing digest-shape predicate to extend. |
| 885 | improvement | scripts/lib/worker-json/predicates.ts | added a rationale comment on isBodyHash explaining its domain-specific scope | Scout Check; guards against a later reader generalising a deliberately narrow predicate (V-YAGNI-03). Scope: within diff. |
| 887 | root-cause | scripts/decision-log-append.ts | Rebuild the Records row block in id-sorted order on every insert instead of tail-appending | Tail-append put every new row at the same anchor line, so 12 of 15 open PRs conflicted simultaneously in this one file; sorted insert separates rows landing in different gaps, verified with a real two-branch git rebase |
| 887 | reuse | scripts/decision-log-append.ts | Reused appendIndexRowIfAbsent's rebuild-the-block idiom rather than importing the function or minting an unrelated second variant | decision-log.md's 5-column schema and numeric sort key mean the parse/render functions genuinely do not reuse across files; the reviewer independently compared both and upheld the divergence as real, not a disguised reimplementation |
| 887 | approach | src/references/merge-conflict-protocol.md | Evaluated and rejected a .gitattributes merge=union entry for decision-log.md as a second leg | The file's frontmatter last_updated field is modify-in-place, not append-only; a whole-file union driver cannot scope itself to the Records table and would silently duplicate last_updated across two dates instead of surfacing a visible conflict |
| 887 | refactor | scripts/lib/check-common.ts, scripts/decision-log-append.ts | Extracted findTableBlock(lines) into check-common.ts; both appendIndexRowIfAbsent and insertRecordRowsSorted call it | Review iteration 1 (F-00074, V-DRY-01 BLOCK): the separator-scan and block-end walk were character-for-character identical and schema-agnostic, unlike the row-parse logic whose divergence the reviewer had upheld; equivalence verified against the real decision-log.md and INDEX.md, and the new helper mutation-tested to prove 11 of 37 tests go red when it breaks |
| 888 | root-cause | templates/hooks/pretooluse/utils/hook-event-log.js | allWorktreeRoots gates fail-open on hasGitMarkerInAncestry(cwd) — a filesystem fact the code verifies itself — instead of on git's exit code | git's fatal() bucket returns exit 128 with an identical 'not a git repository' message for a genuinely absent repo and for a real repo whose HEAD is unreadable, because git's own is_git_directory() treats HEAD-unreadable as part of what disqualifies a directory; no downstream parser can un-conflate a distinction git never made, so any exit-code or message scheme is an enumeration with a next unlisted member |
| 888 | reuse | templates/hooks/pretooluse/validate-file-changes.js | Signal the anomalous-git-failure case by throwing, caught by validate-file-changes.js's existing failClosed() posture, rather than a new sentinel return value | state-write-guard.ts's exit-code-as-contract idiom does not map onto an in-process function, and this file already has an established throw-then-failClosed idiom (readHookInput, loadFilePatterns, the #580 catch-all) for unrecoverable state — reusing it beats inventing a second contract (V-INT-02) |
| 888 | improvement | scripts/lib/forge-adapter/gitea.test.ts, scripts/lib/forge-adapter/gitlab.test.ts | Deleted a test that modelled a consumer that does not exist, rather than keeping it for coverage optics | isMergeBlockedByChecks was defined in the test file and imported nowhere; its own catch{return true} was the only thing under test, so the case could not fail unless that one line were miswritten — a V-TEST-11 vacuity wearing a 'the merge gate' label. Real consumer coverage belongs to #868 once a consumer exists |
| 889 | root-cause | templates/hooks/pretooluse/utils/hook-event-log.js | mainCloneRoot() collapsed every git failure — routine 'outside any repository' and anomalous 'a repository is here and git is broken' alike — into the same null return, which recordEvent read as 'no git context, nothing to record' and silently dropped the durable audit record even for a BLOCK-tier deny that still succeeded. Fixed by mirroring #864/PR #888's hasGitMarkerInAncestry discrimination (a positive filesystem fact, since git's fatal() bucket produces the identical exit code/message for both cases) so mainCloneRoot throws on the anomalous branch, and giving recordEvent its own never-throw local catch plus a CLAUDE_PROJECT_DIR-derived fallback sink (owner-ruled ADR-041 Option C). | Alternatives: Option A (loud stderr only, no fallback sink — record still lost, only visible to a human tailing logs) and Option B (fail-closed on the throw — both blind design critics flagged CRITICAL: failClosed's own recordEvent call re-throws with nothing left to catch, exiting 1, which the harness wrapper's fail-open exit-code contract converts into an ALLOW, the opposite of the intended deny). Option C is strictly additive (writes a record where today none is written, never denies more, never crashes) and was the owner's ruling, including the unconditional-stderr amendment so a fallback success is never silently invisible. |
| 889 | reuse | templates/hooks/pretooluse/utils/hook-event-log.js | Reuse Check: reusing hasGitMarkerInAncestry (hook-event-log.js:116, mirrors allWorktreeRoots:263-272) and isExistingDirectory (hook-event-log.js:175) — 2 hits, both pre-existing in-module utilities. | hasGitMarkerInAncestry is the exact routine/anomalous discriminator #864/PR #888 already established for the sibling allWorktreeRoots resolver; re-deriving it (e.g. inspecting git's exit code/stderr) is not implementable, per that fix's own docstring. isExistingDirectory is the same directory-existence guard readScratchpadDir/readAssignedWorktreeRoot already use for validating an env-var-derived path, so the CLAUDE_PROJECT_DIR fallback is validated identically to every other env-var-derived containment root in this module — no new validation primitive introduced. |
| 889 | improvement | templates/hooks/pretooluse/utils/hook-event-log.js, scripts/lib/test-fixtures.ts | No improvement needed — code already clean. | The touched functions (mainCloneRoot, recordEvent, runPreToolUseHook) already follow the module's established docstring density and naming conventions; the diff's own new docstrings explain the routine/anomalous discrimination and the fallback-sink precedence in place, so no separate naming, error-handling, or dead-code cleanup was warranted within the diff boundary. |
| 891 | root-cause | scripts/plan-quality-gate.ts | Add required, absolute --repo-root to plan-quality-gate.ts and build a repo-root-scoped exists closure passed to the already-injectable findMissingCriticalFiles(section, exists); plan-quality-gate.check.ts itself is unchanged. | The root cause was entirely in the CLI wrapper never using the check module's pre-existing exists injection point, always defaulting to blackhole's own checkout (check-utils.ts's root) instead of the consumer repo — a false-pass on a BLOCKING gate. Fixing the wrapper is the smallest correct diff and matches the check module's existing dependency-injection design. |
| 891 | approach | scripts/plan-quality-gate.ts, scripts/plan-quality-gate.test.ts | Require --repo-root (usage-error exit 2 if absent or relative) rather than silently defaulting to blackhole's own checkout when absent, breaking 3 documented call sites and 5 existing CLI tests, all fixed in this PR. | A silent default to blackhole's own checkout is exactly the false-pass bug this fix exists to close — a fallback would preserve the defect for every caller that forgets the flag. The one-time breakage cost is worth permanently closing the unfalsifiable-gate hazard (V-UNFALSIFIABLE-01). |
| 891 | reuse | scripts/plan-quality-gate.ts | Reused check-review-artifact.ts's required-absolute-path-flag convention (usage-error via path.isAbsolute) inline in plan-quality-gate.ts's own parseCliArgs, rather than extracting a shared helper. | Existence search found only 2 prior occurrences of this pattern (check-review-artifact.ts, merge-base-guard.ts), each implemented inline in its own small parseArgs — below the rule-of-three extraction threshold, so a shared helper would be unearned abstraction for a single boolean check. |
| 891 | improvement | scripts/checks/cwd-pin-guard.check.ts | Updated cwd-pin-guard.check.ts's header comment (stale 'three scripts below' / 'not just the three sites' phrasing) to reflect the now-4-entry TARGET_SCRIPTS list. | The file was already being edited to widen TARGET_SCRIPTS to a 4th script; leaving a now-inaccurate count in its own header comment would hand the next reader a wrong fact for free. |
| 892 | root-cause | scripts/campaign-resume-signal.test.ts | Force the write failure with EISDIR — pre-create writeResumeRequestAtomic's .tmp sibling as a directory — instead of chmod(0o555)'s EACCES | chmod is a no-op for uid 0, so under a root runner the write succeeded and the test asserted against a path it never reached (V-TEST-11 vacuity, not merely a red test); a file-type mismatch is kernel-enforced regardless of uid. Verified by reproducing the original failure under real uid 0 in a container, not by reasoning about it |
| 892 | approach | scripts/campaign-resume-signal.test.ts | Rejected the alternative trigger of a --campaign-dir whose parent is a regular file (ENOTDIR) | fs.existsSync swallows all stat errors and returns false, so evaluateResumeHook takes its early action:'none' exit at the queuePath check before writeResumeRequestAtomic is ever reached — the trigger would never touch the outer catch the test exists to pin |
| 892 | reuse | scripts/campaign-resume-signal.test.ts | Kept the chmod variant as a companion test guarded by test.skipIf(process.getuid() === 0) rather than deleting it | a bare skip under root would leave the fail-open guarantee with no coverage there, converting a V-TEST-10 into a V-TEST-11; keeping both means root reports a stated skip on the permission variant while the EISDIR variant still executes. The skipIf shape reuses scripts/install-verify.test.ts:236's existing idiom rather than minting a second one |
| 894 | root-cause | scripts/lib/build/content.ts, scripts/lib/build/facts.ts | Gate {{INCLUDE}} expansion inside expandIncludes on a declared INCLUDE_MARKER_SITES file allowlist (ADR-039, Option C) | read() is one of three entry paths into the primitive; a guard at read() protects one door of three. A file allowlist is closed by construction — it grows only when an agent deliberately adopts the seam — where the rejected path-prefix option named an open-ended region that is precisely where prose about the marker gets written |
| 894 | approach | scripts/build.test.ts | Used the existing siteList parameter and push/splice-in-finally conventions for the build-test fixture seam, not the env-var seam the plan prescribed | the plan's premise that T6 shells out to `bun run build` did not match the test, which calls compileFolder/compileGeminiTree/compileCodexTree in-process; an env-var seam would have been unused scaffolding (V-YAGNI-01) and a second enforcement shape (V-INT-02) built on an unchecked premise |
| 894 | refactor | scripts/lib/build/facts.ts | Trimmed historical narrative to fit the new declared fact under the hard 300-LOC ceiling rather than raising the ceiling | the file's own comment forbids raising it (pinned rather than seeded so the file answers to one ceiling); the trimmed text was archaeology the Comment Discipline rules already class as removable (V-DOC-06), and the stale {{...}}-quoting workaround comment was reworded to state the ADR-039 rule that made the workaround unnecessary |
| 894 | improvement | documentation/reference/check-utils-blast-radius.md | Fully reconciled the consumer table (added 20 missing rows, 52 live importers) instead of patching the three disagreeing headline numbers | live re-measurement found the drift was larger than the task described; a headline-only patch would have relocated the same hand-maintained-counts-drift failure this doc's own history log already shows happening three times |
| 895 | root-cause | templates/hooks/pretooluse/utils/bash-context.js, templates/hooks/pretooluse/utils/worktree-removal-guard.js | Root cause: computeMaskedSpans's own quote handling wholesale-skips a non-print-sink double-quoted span via skipQuotedSpan without ever inspecting it for a nested heredoc, so its existing heredoc-body masking never fires when the heredoc sits behind "$(...)" (e.g. "$(cat <<'EOF' ... EOF)"). clauseTailFrom then extracted that heredoc's raw prose verbatim, letting containsWorktreeRemoveTokens match a literal worktree/remove word pair inside it. Fix: an additive computeHeredocBodyMask export (bash-context.js) — an independent scan that does not skip double-quoted spans, reusing consumeHeredoc/collectHeredocOperatorsOnLine/parseHeredocDelimiter/maskLiteralSpan/isEscapedQuote verbatim — threaded through skipDollarParenSpan (paren-depth skip) and clauseTailFrom (output blanking) only. findClauseStartIndices (ADR-040's quote-UNAWARE clause splitter) is untouched. | Chose to fix the actual masking gap over pattern-matching the reported repro string, so the fix generalizes to any heredoc nested in a $(...) argument, not just issue #895's exact wording. Reusing the audited private heredoc helpers verbatim (rather than writing a second heredoc-boundary detector) avoids two independently-drifting implementations of the same #506 boundary logic (V-INT-02). Verified via 3 behavioral fixtures (wrongly-refused-to-allow, adjacent-must-stay-refused, F-00065 eval-bypass regression) plus 3 unit-style mask assertions, all green; red state captured verbatim pre-fix for the primary fixture. Confidence: High. |
| 895 | reuse | templates/hooks/pretooluse/utils/bash-context.js, templates/hooks/pretooluse/utils/worktree-removal-guard.js, scripts/hooks-validate-bash.test.ts | Reuse Check: reusing bash-context.js's private heredoc helpers verbatim (isEscapedQuote:64, maskLiteralSpan:135, parseHeredocDelimiter:196, collectHeredocOperatorsOnLine:238, consumeHeredoc:290) — 1 prior consumer (computeMaskedSpans itself). Repo-wide existence search for computeHeredocBodyMask/heredocMask found zero prior occurrences (first occurrence of this concern). Separately, the new unit-style bash-context.js assertions reuse the established in-process-require convention from scripts/hooks-validate-file.test.ts:1129 (mainCloneRoot unit tests, #889) rather than inventing a new one. | V-INT-02 — a second, independently-maintained heredoc-boundary detector would drift from the audited #506 one over time; reusing the exact same helpers guarantees identical body-boundary semantics by construction. |
| 895 | improvement | templates/hooks/pretooluse/utils/bash-context.js, templates/hooks/pretooluse/utils/worktree-removal-guard.js | Scout Check: no improvement needed beyond the fix itself — code already clean. Both changed functions (skipDollarParenSpan, clauseTailFrom) already carried thorough docstrings before this diff documenting their naive/quote-unaware limitations; this diff's own additions extend those docstrings in the same voice rather than leaving a gap. | Forcing an unrelated change to already-clean, well-documented code in this diff's touched functions would be scope creep against V-SCOPE-01/KISS. |
| 896 | approach | templates/hooks/pretooluse/utils/shell-lexer.js, templates/hooks/pretooluse/utils/bash-context.js, templates/hooks/pretooluse/utils/bash-write-target-guard.js, templates/hooks/pretooluse/utils/worktree-removal-guard.js | Extract only the uniform primitives (skipQuotedSpan x5, isRedirectAmpersand x3) into a shared shell-lexer.js; deliberately do NOT unify the three guards' clause splitters. | The splitters need OPPOSITE quote policies: worktree-removal-guard requires quote-UNawareness (only an unaware splitter reaches the && inside eval "cd <parent> && rm -rf <basename>"), while bash-write-target-guard requires quote-AWAREness (without it, sed -i 's/a;b/c/' <main-clone-path> fragment-splits and the write is allowed). A union splitter was scored and rejected; it carried the design evaluation's only two CRITICALs, filed independently by two blind critics. ADR-040. |
| 896 | reuse | templates/hooks/pretooluse/utils/shell-lexer.js | 5 bespoke quote-span-skip occurrences and 3 bespoke isRedirectAmpersand occurrences consolidated into one shared module; parseHeredocDelimiter's loop deliberately left inlined. | Rule-of-three threshold passed for both primitives. parseHeredocDelimiter is a different algorithm with no backslash-escape handling (it parses a delimiter word, not a shell string); migrating it would change behaviour on an escaped-quote-in-delimiter input. Independently confirmed correct by the reviewer reading both loops side by side. |
| 896 | improvement | templates/hooks/pretooluse/utils/shell-lexer.js | Deleted the SEPARATORS constant and its export. | Dead on arrival: grep across templates/ scripts/ src/ returned exactly 2 hits, its own definition and its own export. Not referenced even inside its own file. V-YAGNI-03 / V-KISS-03. |
| 896 | root-cause | src/references/blackhole-protocol.md | Corrected the documented list of files reimplementing shell-lexer primitives: pattern-loader.js replaced by bash-write-target-guard.js. | pattern-loader.js is 72 lines with zero quote-skip, clause-split or shell-scanning code; the original claim was factually wrong. F-00078, V-DOCFACT-01. |
| 901 | reuse | scripts/lib/check-common.test.ts | reusing the existing test.each characterisation matrix (check-common.test.ts:294) | One hit; no new test infrastructure or utility introduced — the two degenerate rows slot into the matrix that already covers the other five shapes. |
| 901 | improvement | scripts/lib/check-common.test.ts | no improvement needed — code already clean | Scout Check: the new rows and the extended comment follow the file's existing conventions exactly. |
| 901 | other | scripts/lib/check-common.test.ts | canonicalIndexPath keeps its best-effort unwrap; no schema-shape rejection added (AC3/AC4 resolution, recorded inline) | No current RootIndexRow producer can emit either degenerate shape today, but parseIndexTableRows filters only a literally-empty raw cell — not a link that canonicalizes to empty or garbled — so that is a claim about today's producers, not a structurally enforced invariant. A stricter regex risks re-breaking the parenthetical self-referential-link case the href widening exists to fix, and no consumer is affected, so the trade needs its own evidence rather than riding along here. |
| 902 | reuse | scripts/lib/argv-flags.ts, scripts/stack-repair.ts | Reuse Check: repo-wide existence search found exactly one prior implementation of this concern — scripts/stack-repair.ts:187-204's private parseFlags/requireFlag. Extracted it verbatim into a new shared scripts/lib/argv-flags.ts module and adopted it at 15 call sites instead of writing a new parser; stack-repair.ts itself is deliberately not migrated in this PR (out of router-scoped leg boundary), leaving its own copy a disclosed 16th duplicate pending a fast-follow. | 1 hit (below rule-of-three) — reusing the closest match by extraction rather than inventing a second implementation of the same ~10-line idiom (V-INT-02/V-DRY-01). |
| 902 | improvement | scripts/check-review-artifact.ts, scripts/promote-review-artifact.ts | Scout Check: replaced pre-migration non-null assertions (args[key]!) in check-review-artifact.ts's and promote-review-artifact.ts's parseArgs with explicit typeof-narrowed checks ahead of the return, so every required field is verified as a string before use instead of asserted. | Both functions were already being touched by the migration itself; removing an untyped assertion from code already in the diff costs nothing extra and closes a small type-safety gap without expanding scope beyond the two functions already being edited. |
| 906 | approach | scripts/lib/check-common.ts | Relocated a corrected decision-record note onto the new findAdrFileByNumber export in check-common.ts rather than deleting the old design-aggregate.ts comment along with the code it documented | The old comment recorded issue #775's deliberate keep-local decision and asserted a links.check.ts duplicate that never existed (F-00097, V-DOCFACT-01); deleting it with the code would have erased both the record and the false claim with no correction anywhere, the same failure mode that let the false claim propagate from the source comment into issue #867's body and then into #903's own filed body |
| 906 | reuse | scripts/lib/check-common.ts | Reuse Check: reusing check-common.ts's existing shared cross-domain helper convention (walkMdFilesAbs, findMissingGateMarkers) — findAdrFileByNumber joins the same file as one more small export | check-common.ts already hosts cross-domain helpers imported by both *.check.ts modules and plain scripts, and carries no enforced CONTENT_GATE_BUDGETS ceiling — a safe, pre-existing host, not a new module (1 hit, established convention) |
| 906 | improvement | scripts/lib/carry-staged-artifacts.ts | Extracted the new_file and append_row branches' near-identical mkdir/write/catch blocks into one private writeCarryTarget helper | Both blocks were identical except the written variable (content vs result.content); the extraction removed 8 duplicated lines and centralizes the write-failure message construction the V-AUTO-02 audit reads verbatim, at zero behavior change (pinned by characterization tests written before extraction, including a fixture self-check proving the append_row test discriminates a swallowed-vs-unswallowed failure) |
| 907 | approach | templates/hooks/pretooluse/utils/hook-event-log.js | Capture agent_id/agent_type once per process in module-scoped state inside readHookInput, and read it back in recordEvent, rather than threading a param through all nine denyAndRecord/warnAndRecord call sites. | Each hook script is a fresh single-payload subprocess (no cross-request leakage risk), so module state stays correct; threading a param would touch every call site in both validators for two fields their own decision logic never inspects — smaller diff, same guarantee. |
| 907 | reuse | templates/hooks/pretooluse/utils/hook-event-log.js | None found — first occurrence of extracting agent_id/agent_type from a PreToolUse payload (repo-wide search). | No prior code in the repo reads these fields; the new capture/record path is genuinely novel, so nothing existing could be reused. |
| 907 | improvement | templates/hooks/pretooluse/utils/hook-event-log.js, scripts/lib/build/claude-native-settings.ts | No improvement needed — code already clean. | The touched functions (readHookInput, recordEvent, buildCommand) already carry precise docstrings and a single clear responsibility each; the new fields extend the same pattern without introducing anything that needed cleanup. |
| 908 | root-cause | scripts/lib/hook-event-triage.ts | Finding identity was keyed on the event JSON's own generated filename (file: relFile in findingDedupKey), which is unique per event by construction, so dedup collapsed nothing and 392 events would have produced ~338 BLOCK rows. | The visibility gap was never a conflict with never-drop-a-finding; it was a mis-specified finding identity. Re-keying on (vcode, pattern_id, worktree) dissolves the collision instead of trading against it: all 392 events are archived and represented as <=15 rows whose occurrences sum to 392 exactly. |
| 908 | approach | scripts/lib/hook-event-triage.ts, scripts/lib/campaign-status/dashboard.ts | Owner-ruled A-prime: per-class identity carrying occurrences/last_seen_at, open-only dedup, archive-then-delete, plus rendering the count. ADR-042. | design-aggregate.ts returned blocked with three scorers picking three different winners (primary->A 10.6%, critic_a->C 15.0%, critic_b->D 20.0%). Changing the unit of counting for a BLOCK V-code is the same class of change as changing its severity, so it went to the owner rather than being settled in a plan. Both critics independently found that occurrences would be written and never read (the renderer emits no count field) and that isDedupCandidate admits deferred while the renderer shows open only - making a deferred row a permanent silent sink. Both fixes are part of the ruling. |
| 908 | reuse | scripts/lib/hook-event-triage.ts, src/references/orchestrator-runtime.md | Reused the Doc-Health / Plugin-Drift turn-start cadence and existence-gating shape for the new ingestion trigger; did NOT reuse their advisory-only property. | ingestHookEvents was real, wired and tested but referenced in exactly one place - batch-Triage step 1b - so any hook event outside an actively-triaged worker batch waited indefinitely. The cadence transfers; the advisory property does not, because those two write a signal file while this mutates the ledger under the single-writer invariant. |
| 908 | improvement | scripts/lib/build/claude-native-settings.ts | Enriched the wrapper's error-tier detail with a stderr tail and a timeout/crash discriminator, diverging from ADR-042's literal output=$(cmd 2>&1) sketch. | All 227 error-tier records carried the identical literal 'process exit code 1' and ingestHookEvents never read event.detail at all, so every scored option would have raised a louder alarm with zero actionable payload. The ADR's sketch was not correct as written; the divergence is documented in the PR body rather than applied silently. |
| 909 | root-cause | scripts/lib/hook-event-triage.ts, scripts/lib/hook-event-triage.test.ts | Reorder side effects in hook-event-triage.ts: strip archiving out of ingestHookEvents (now filesystem-side-effect-free, returns consumedFiles[]); add exported archiveConsumedFiles({repoRoot, consumedFiles}); main() calls it only after fs.renameSync(tmpPath, ledgerPath) succeeds. | Root cause: ingestHookEvents archived each event file in-loop (fs.renameSync at old line 193) before the accumulated ledger update was ever persisted (fs.renameSync(tmpPath, ledgerPath) in main() at old lines 245-256). A crash or guard refusal between those two writes silently lost the finding of every already-archived event, with no re-surfacing mechanism. Alternatives: persist after every single event (Option 2, rejected — turns 1 ledger write into up to N, ~392x cost on the measured corpus); two-phase commit with an intent marker + reconciliation (Option 3, rejected — adds a second on-disk protocol and reconciliation path disproportionate to the residual risk once persist-then-archive bounds it, V-KISS-01). Chosen fix converts an unrecoverable, invisible failure into a recoverable, visible, bounded one (a crash in the new narrower window between ledger install and archiving re-ingests the same file next run, inflating occurrences by 1 — self-correcting, zero added write cost). |
| 909 | reuse | scripts/lib/hook-event-triage.ts | Reuse Check: none found — first occurrence of "move a batch of consumed files into a timestamped .blackhole/archive/<prefix>-<ts>/ directory" as a standalone extracted primitive (repo-wide). | Existence search covered scripts/lib/*.ts and scripts/*.ts for archive-directory-with-timestamp patterns. Two near-miss candidates exist (scripts/migrate-ledger-schema.ts's runMigration snapshot helper, and hook-event-triage.ts's own main() ledger-snapshot copy) but both copy a single file into archive/ as a pre-mutation snapshot, a different concern from moving N already-processed source files out of an intake directory. No rule-of-three extraction candidate — reusing neither; archiveConsumedFiles is genuinely new, extracted verbatim (unchanged behavior) from the prior in-loop code, not hand-rolled fresh. |
| 909 | improvement | scripts/lib/hook-event-triage.ts | Scout Check: added an inline comment at the archiveConsumedFiles call site in main() naming the chosen option (Option 1, issue #909) and the accepted duplicate-ingest tradeoff, replacing the old comment's now-stale claim that archiving happens per-event inside ingestHookEvents. | The touched code's only pre-existing comment (the ADR-042 item 4 archive-then-delete note above the old in-loop rename) described a side effect that no longer lives at that call site after the fix; leaving it in place would have been a stale comment pointing at moved code. Replaced with a comment that documents the ordering guarantee actually being relied on, at the actual call site — within diff boundary. |
| 912 | root-cause | scripts/plugin-drift-signal.ts, scripts/lib/plugin-drift.ts | Root cause: plugin-drift-signal.ts built exactly one installed-cache path from the repo's OWN package.json version, so a stale copy installed under a DIFFERENT version (0.19.0 vs repo 0.21.8) was invisible to the signal — reported installed_present: false while that copy actively vetoed calls. Alternatives considered: Option B (resolve the installed version from installed_plugins.json, keep single-source shape) rejected — both blind design critics independently found it adjudicates an unverified scope-resolution assumption and can render a wrong row's hooks_hash_match: true, a silent false negative. Option C (enumerate all sources, assert no ordering) rejected on its own critic's finding: hashing the repo's build output against itself is an unfalsifiable green (V-UNFALSIFIABLE-01) for the project-native source, which is 14 commits behind origin/main today. | Adopted Option A (owner ruling on a design-aggregate.ts blocked verdict): enumerate every registered PreToolUse source (scripts/lib/hook-sources.ts) and assert an ordering only where a resolvable git commit SHA proves one (scripts/lib/hook-source-ordering.ts), never guessing from a version string. Fixes the symptom (guessed path) at its structural root (one-path guess conflated 'what is registered' with 'how do the registered things compare'). |
| 912 | reuse | scripts/lib/hook-sources.ts, scripts/lib/hook-source-ordering.ts, scripts/plugin-drift-signal.ts, scripts/hook-sources.test.ts | Reuse Check: reusing hashDirectory (scripts/lib/plugin-drift.ts:25), readOriginUrl (scripts/lib/forge-detection.ts:26), and withTempGitRepo/runGit (scripts/lib/test-fixtures.ts:179/147) rather than reimplementing directory-content hashing, origin-remote detection, or real-git test fixtures. Existence search (repo-wide) found no prior HookSource-shaped enumeration or a merge-base --is-ancestor ordering helper; scripts/merge-base-guard.ts established the local git-shelling-with-injected-resolver idiom this design's GitResolver follows, but its ancestry check is single-direction and scoped to PR-merge-base assertions, not a reusable bidirectional ordering primitive — first occurrence of a bidirectional commit-ancestry ordering module in this repo. | V-INT-02 — never reimplement an existing codebase utility. hashDirectory/readOriginUrl/withTempGitRepo/runGit are each adopted verbatim; the enumeration and ordering modules are genuinely new (no rule-of-three trigger — 0 prior bespoke occurrences of either concern). |
| 912 | improvement | scripts/campaign-status.ts | No improvement needed — code already clean. renderPluginDriftWarning was fully rewritten in this diff (single-boolean gate to per-source + veto_pairs rendering); the surrounding exports and dashboard wiring in campaign-status.ts were not touched and show no naming, error-handling, dead-import, or stale-comment issue within this diff's boundary. | Scout Check is unconditional but does not mandate a forced change — the reviewer verifies the entry's presence, not a change, per mercure-scout-protocol.md and implementer.md § Scout Check. |
| 913 | root-cause | scripts/checks/read-json-file.check.ts, scripts/verify.read-json-file.test.ts | Scrub comment/string-literal content from each line before testing BARE_PARSE_RE, instead of tightening the regex or skipping comment-only lines. | Root cause is that BARE_PARSE_RE matched raw text with no lexical awareness; a regex-only tweak (exclude // prefix, require assignment position, skip comment-only lines) closes at most half the failure class since a string literal in any position still matches. A minimal per-line character-walk scrubber addresses both the comment and string-literal cases without adding a parser dependency (none is installed). |
| 913 | reuse | scripts/checks/read-json-file.check.ts | First occurrence of a comment/string stripper under scripts/ (repo-wide search for strip*comment/tokeniz/AST import found none) — implemented inline, not extracted to scripts/lib/. | jq-empty-guard.check.ts scans markdown prose via walkMdFilesAbs, where in-comment/in-string is not a meaningful question, so it has no use for this stripper; extracting one now for a single consumer would be a V-YAGNI-03 single-consumer abstraction. |
| 913 | improvement | scripts/checks/read-json-file.check.ts, scripts/verify.read-json-file.test.ts | No improvement needed — code already clean. | The touched functions (stripCommentsAndStrings, findBareJsonParseBypasses) are small, named, independently readable, and the new tests follow the existing describe/test fixture shape exactly; nothing else in the touched region warranted a scout-check change. |
| 917 | reuse | src/agents/planner.md, src/references/orchestrator-delegation.md | none found — first occurrence of a cite-the-production-call-site rule; framed as a sibling of the existing ADR citation check (planner.md, issue #775) and the verbatim-carry convention for contended-path exclusions (orchestrator-delegation.md), reused in framing rather than duplicated | Repo-wide grep for 'production call site' / 'data-structure orientation' returned nothing prior. Both siblings already establish the shape — verify a citation against its live source before relying on it, and carry it forward verbatim rather than re-paraphrasing — so this extends two existing conventions instead of adding a third. |
| 917 | improvement | src/agents/planner.md | no improvement needed — code already clean | Scout Check on a prose-only diff within the touched Step 6 region. |
| 917 | other | src/agents/planner.md | the new bullet carries no V-code prefix — labelled 'Data-structure orientation citation (issue #917)', matching the ADR citation check's style | The plan's mandated prose labelled it V-INT-03, which is a pre-existing code meaning 'no third variant of a solved concern' and is already cited four times in this same file in that correct sense. The plan's 'no new V-code' constraint meant do not mint one, not borrow an unrelated one; two meanings for one identifier in one file is worse than no identifier. The implementer implemented the prose verbatim as instructed and surfaced the collision rather than deviating silently or shipping it. |
| 919 | reuse | src/references/hook-schemas.md | No new utility, helper, or abstraction introduced — pure prose edits to three existing paragraphs plus one new section | Existence-search sub-search is scoped to new-utility introductions (Reuse Check Gate); this diff is prose-only, so V-INT-02 is N/A, matching the plan's own V-code Compliance section |
| 919 | improvement | src/references/hook-schemas.md | No improvement needed beyond the plan's own three edits — added a missing blank line before the new ## PermissionDenied heading to match the file's existing heading spacing convention | Scout Check requires one in-scope improvement or an explicit 'already clean' note; the blank-line fix keeps the new section visually consistent with every other top-level heading in the file |
| 921 | root-cause | src/references/blackhole-state.md, src/agents/planner.md, src/agents/investigator.md | Compute the full destination path first, write the heredoc body to <that-full-destination-path>.tmp, then atomically mv it into place, replacing the issue's proposed discipline-based fix (agents remember issue-prefixed scratch filenames). | Root cause: two concurrently running agents each chose an unnamespaced, convention-based scratch filename (a bare /tmp/*.md path) rather than deriving the scratch name from an already-unique destination path. Alternatives considered: (1) the issue's own discipline-based proposal, rejected because it relies on every future agent remembering a naming convention with no structural guarantee; (2) a file-locking/flock mechanism around scratch writes, rejected as new concurrency machinery for a problem solvable by naming alone. Destinations are already namespaced by issue (.blackhole/staged/<issue>/, plans/issue-N.md), so deriving the .tmp name from the destination inherits that namespacing for free and closes the collision class by construction. |
| 921 | reuse | src/references/blackhole-state.md | Reusing <file>.tmp write-then-atomic-mv (blackhole-state.md § Write protocol, queue.json/findings-ledger.json) — 2 hits. | The new § Staging rule adopts this exact naming shape rather than inventing a second .tmp convention (V-INT-03). Existence search covered scripts/lib/state-write-guard.ts's existing convention and this plan's own new rule text; both use the identical extension-suffixed <file>.tmp shape. |
| 921 | improvement | src/agents/planner.md, src/agents/investigator.md | No improvement needed — code already clean. | This is a citation-only, prose-scoped diff; the touched sites (existing heredoc-write descriptions) required no naming, error-handling, or dead-code cleanup beyond the citation insertion itself. |
| 923 | approach | scripts/backfill-adr-frontmatter.ts | Splice only the missing lifecycle keys into each ADR's existing frontmatter lines (canonical relative order), instead of literally regenerating a fresh 6-line frontmatter block as the plan's prose described. | A full-corpus scan showed 32 of the 37 target ADRs carry non-required frontmatter keys (`related:` multi-line lists, `tracking_initiative:`, `scope:`, `supersedes:`, `superseded_by:`). A literal 6-line regeneration would silently delete all of them, violating constraint 1 ("preserve every existing value"). Surgical splicing satisfies every stated AC while adding a stronger safety property the plan's prose under-specified. |
| 923 | reuse | scripts/backfill-adr-frontmatter.ts | Reused parseMdFrontmatter, parseFrontmatterFields, parseIndexTableRows, and the JSON.stringify/JSON.parse YAML-scalar idiom from scripts/lib/build/content.ts and scripts/lib/check-common.ts rather than writing new parsers. | Same touchpoint (ADR-031 precedent scripts/migrate-doc-index-summaries.ts) already established these as the canonical primitives; parseFrontmatterFields (multi-key reads) is the more-adopted convention (doc-index-generate.ts, carry-staged-artifacts.ts, adr-supersession.check.ts) versus the precedent script's single-key regex helper, so it was chosen for this 6-key read (V-INT-01/V-INT-02). |
| 923 | improvement | scripts/backfill-adr-frontmatter.ts | No improvement needed beyond the primary change — code already clean. | New script, TDD from empty; nothing pre-existing in this touchpoint to improve beyond what the primary change already implements. |
| 928 | root-cause | scripts/lib/campaign-status/state.ts, scripts/campaign-status.ts | Guard-before-load: loadCampaignState checks fs.existsSync(campaignDir) before its three readJsonFile calls and throws an exported CampaignNotFoundError; main() catches only that type and prints+returns (exit 0), rethrowing anything else uncaught. | readJsonFile catches ENOENT and SyntaxError into one generic Error with no error.code/cause preserved and has 16 other call sites depending on its throw-with-label behavior, so catch-and-classify could only distinguish absent-from-corrupt by substring-matching ENOENT. Checking existence before the read (reusing doctor.ts:160-168's checkConfigExists shape, not its DoctorCheck return type) keeps readJsonFile untouched and keeps a corrupt config.json crashing exactly as before. |
| 928 | reuse | scripts/lib/campaign-status/state.ts | reusing checkConfigExists's fs.existsSync guard-before-load shape (scripts/doctor.ts:160-168) for the new CampaignNotFoundError guard in loadCampaignState | Repo-wide grep for existsSync(campaignDir)/*NotFoundError* found no existing directory-existence guard or not-found error class for a campaign directory anywhere in scripts/ — first occurrence; reused doctor.ts's established pattern rather than inventing one. |
| 928 | improvement | scripts/campaign-status.ts, scripts/lib/campaign-status/state.ts | no improvement needed — code already clean | The touched region of both files was freshly authored for this fix with load-bearing comments explaining the guard-before-load rationale; no pre-existing naming, error-handling, dead-import or stale-comment issue in the surrounding touched lines. |
| 929 | reuse | scripts/lib/campaign-status/dashboard.ts | reusing renderLedgerOpenSection's existing cap-and-overflow idiom — extracted into a shared SECTION_CAP/capOverflowLine pair and applied to all three list sections, including refactoring the Ledger section itself to consume it | A repo-wide search for a second '…and N more' idiom or an unrelated .slice(0, 10) cap found none, so this is the sole existing pattern. Refactoring the working section to use the extraction is what makes it genuine reuse rather than a third copy (V-INT-03); its five pre-existing tests pass unchanged, proving the extraction is output-identical. |
| 929 | improvement | scripts/lib/campaign-status/dashboard.ts | no improvement needed — code already clean | Scout Check: the touched functions were already narrowly scoped and consistently commented; the extraction itself is the improvement this issue asked for. |
| 929 | other | scripts/lib/campaign-status/dashboard.ts | the verdict is labelled 'Queue health:' rather than a bare 'Campaign:', and neither plugin-drift nor doc-health feeds its inputs | renderPluginDriftWarning is called from campaign-status.ts after formatDashboard and prints unconditionally, so a verdict computed inside formatDashboard always renders above it — an unqualified HEALTHY directly above a drift warning would misrepresent its own scope. Doc-health is not rendered by bun run status at all. Narrow labelling makes the verdict one signal among several rather than a summary claiming to subsume them. |
| 942 | root-cause | scripts/decision-log-append.ts, scripts/decision-log-append.test.ts, documentation/reference/decision-log.md | Add assertNoEmbeddedNewline guarding the three escapeCell call sites (reject, non-zero exit distinct from usage()'s 2) and an exported findRecordsTableViolations structural check scanning to end-of-body rather than findTableBlock's blockEnd, then repair the split 885 row and reorder the three 929 rows to the table tail. | escapeCell escaped only \|, so a cell value carrying a real embedded newline (origin: a JSON \n escape becoming a literal newline before reaching the function) was written verbatim, splitting the row. findTableBlock's blockEnd then treats the orphaned tail as outside the table, so every insert after the corruption silently appended past it (ids 885/887/888 sorted after 929 despite 929 being highest). A guard alone would not fix the already-corrupted live file; a structural check alone would not prevent recurrence — both legs are required, plus the one-time data repair. |
| 942 | reuse | scripts/decision-log-append.ts | Reuse Check: none found -- first occurrence of an embedded-newline/carriage-return guard, repo-wide | Repo-wide grep for assertNoEmbeddedNewline / embedded newline guard logic outside decision-log-append.ts found no existing implementation. Implemented as a small local function per the plan's Design Decision (V-YAGNI-03, single consumer today) rather than extracting a shared module, since issue #941's check-common.ts target is a different table schema with no current instance of this defect. |
| 942 | improvement | scripts/decision-log-append.ts | No improvement needed beyond the primary fix -- code already clean | Scout Check: the touched region (assertNoEmbeddedNewline and findRecordsTableViolations) is newly authored, single-purpose, and documented at its definition site with load-bearing rationale comments; no dead code, stale comment, or naming issue was found in the touched region beyond what the fix itself already addresses. |
| 942 | refactor | scripts/decision-log-append.ts | Deviated from the plan's literal check (a) regex (/^\\|\s*\d+\s*\\|/, a purely-numeric id cell) to a simpler leading-pipe check (line.startsWith('\|')), documented inline | The live decision-log.md carries 4 pre-#874 legacy compound-id rows (\| PR #428 / #421 \| ...) that are well-formed but not digit-first; recordSortKey already tolerates this shape for sorting. The plan's literal regex would have flagged these real, unrelated, correct rows as violations, permanently failing the live-file test regardless of the repair. The leading-pipe check targets the actual signal an orphaned continuation line lacks (no leading \|) without misfiring on legitimate legacy data. |
| 944 | root-cause | scripts/checks/new-file-test-pairing.check.ts, scripts/new-file-test-pairing.ts, src/agents/reviewer.md, src/references/audits/02-tdd-testing-baselines.md | Mechanical backstop (scripts/checks/new-file-test-pairing.check.ts + scripts/new-file-test-pairing.ts CLI) plus a named Iron Law row and a strengthened module 02 bullet, extending existing V-TEST-01/02 with no new V-code and no new audit module. | Higher short-term build/test cost than a prose edit, but only the mechanical version is provably falsifiable (red-before-green fixture, dogfooded against the live 54-file corpus) and closes the recurring failure class (a worker skips a test, the reviewer is talked past it) regardless of why the test is missing this time. |
| 944 | reuse | scripts/checks/new-file-test-pairing.check.ts, scripts/new-file-test-pairing.ts | Reuse Check: reusing findMissingGateMarkers/readComposedAgentDoc (scripts/lib/check-common.ts:17) and parseFlags (scripts/lib/argv-flags.ts) — 2 hits, following the module-30 (v-test09-hooks-claim) precedent exactly. | The grounding-marker scan and CLI argv-parsing concerns already have one established repo-wide utility each; a second implementation of either would be a same-diff duplicate (V-INT-02/V-DRY-01). |
| 944 | improvement | src/agents/reviewer.md, src/references/audits/02-tdd-testing-baselines.md | Improvement Record: no improvement needed — code already clean. The diff's touched pre-existing files (src/agents/reviewer.md, src/references/audits/02-tdd-testing-baselines.md) received only pure additions (a new table row, a new bullet); no existing line in either file was modified. | Scout Check applies to already-touched code; since both edits are pure appends with no pre-existing content altered, there was nothing in the touched region to improve. |
| 946 | root-cause | scripts/lib/carry-staged-artifacts.ts, documentation/audits/analysis-issue-863.md, documentation/investigations/hook-event-fail-open-visibility.md, documentation/investigations/research-cross-plugin-hook-denial-visibility.md | Task 1 precondition failure (11-row drift between committed and generated documentation/INDEX.md) root-caused to carry-staged-artifacts.ts's rewriteInvestigatorFrontmatter never writing a summary: field onto a promoted investigator note; fixed the write path (Task 7c) in addition to backfilling the 3 already-affected files' frontmatter, so the same drift cannot recur on the next investigator carry. | A data-only backfill (editing just the 3 files) would have left issue #869's already-staged note (.blackhole/staged/869/analysis-issue-869.md) as drift row 12 the moment its PR carries. Fixing the write path closes the recurrence, not just the symptom. |
| 946 | reuse | scripts/lib/doc-index-generate.ts | Reuse Check: none found — first occurrence of a structured two-array row-diff/classification utility (repo-wide). diffDocIndexRows (scripts/lib/doc-index-generate.ts) is a new export. | Existence search found only domain-specific set-difference one-offs (queue-coherence.check.ts's findOrphanedInFlightWorktrees, stop-mode.check.ts's orphan filter) — neither operates on RootIndexRow content equality, so no existing utility fit closely enough to reuse without distorting its own domain. |
| 946 | improvement | scripts/checks/doc-health.check.ts | Scout Check: removed the now-dead isDecisionsDoc predicate from doc-health.check.ts after the V-DOCHEALTH-01/02 retarget stopped calling it, instead of leaving it as unreferenced dead code. | Within the diff boundary — the predicate was only ever used inside findOrphanDocs, which this diff rewrote to consume diffDocIndexRows directly. |
| 948 | root-cause | documentation/audits/build-tree-install-resolution.md | Edit the hand-written prose in documentation/audits/build-tree-install-resolution.md directly rather than touching any script or COMMITTED_TARGET_TREES declaration. | Confirmed via git grep at plan_base_commit that no script under scripts/** references this file, so nothing regenerates its content; it is a one-time #328 audit deliverable, so a prose edit is the correct fix and a generator/declaration edit would target a system that does not drive this file. |
| 948 | reuse | documentation/audits/build-tree-install-resolution.md | Reuse Check: none found - this is a hand-authored documentation prose correction with no reusable utility or generator to check against. | The existence search (git grep for scripts referencing this file) came up empty per the plan's own Root-cause finding, and the convention search found the file has no established generation pattern to follow; the fix is a direct prose edit consistent with the file's own nature as a one-time audit deliverable. |
| 948 | improvement | documentation/audits/build-tree-install-resolution.md | Reformatted all four Classification-summary bucket bullets to a consistent bold(label+count) style, e.g. **Maintainer-surface (2)**, instead of the prior mixed style that bolded only the label with the count outside. | The prior mixed style (bold label, unbolded count) would have broken the plan's own grep ACs for the two new buckets (a literal substring match on "Maintainer-surface (2)" cannot cross a ** boundary); applying the same consistent style to all four buckets, not just the two new ones, keeps the section internally uniform rather than introducing a second inconsistency to fix the first. |
| 949 | reuse | scripts/checks/vcode-parity.check.ts; scripts/lib/mercure-vcode-snapshot.ts | Reuse Check: reusing parseVcodeTableRows and expandVcodeTableKey (scripts/lib/check-common.ts:107-138) for the blackhole side of the parity comparator; wrote a new parseMercureVcodeTable (scripts/lib/mercure-vcode-snapshot.ts) for mercure's distinct 3-column table shape, following the same split-and-trim technique as an established sibling parser, not a new technique | Existence search across scripts/lib and scripts/checks found exactly one prior blackhole-side table parser (parseVcodeTableRows/expandVcodeTableKey) already reused by vcode-severity-sync.check.ts and vcode-citation.check.ts; no prior parser exists anywhere in the repo for mercure's 3-column ID/Severity/Description shape, so this is the first occurrence of that concern (repo-wide) and a new function was justified rather than a rule-of-three extraction |
| 949 | improvement | scripts/lib/build/facts.ts | Scout Check: compressed two pre-existing multi-line JSDoc comments in scripts/lib/build/facts.ts (QUEUE_STATUSES, QUEUE_NOTES, IMPLEMENTER_GATE_MODULE_COUNT) by re-wrapping at a wider line width with zero content loss, to make room for the new MERCURE_VCODE_SNAPSHOT_STALE_DAYS fact within the file's own grandfathered 300-LOC ceiling (CONTENT_GATE_GRANDFATHERED) | facts.ts was already sitting exactly at its 300-LOC grandfathered ceiling at plan_base_commit; adding any new declared fact without an offsetting trim would have pushed the file over V-CONTENTGATE-01's hard gate. The trims are pure re-wraps of comments already inside the file this task's own Touch-Path declares, not a scope-expanding refactor of unrelated code (V-SCOPE-01) |
| 954 | root-cause | scripts/decision-log-append.ts | Wire the existing validateDecisionRecordsArray into appendDecisionRecords instead of writing a new validator or patching shared-validators.ts | The validator, its shape, and its docs already exist and are correct for this consumer; a second validator would duplicate logic that drifts, and shared-validators.ts has other consumers outside this issue's Touch-Paths |
| 954 | reuse | scripts/decision-log-append.ts | Reusing validateDecisionRecordsArray (scripts/lib/worker-json/shared-validators.ts:96) | validate-worker-json.ts's implementer path already calls it; decision-log-append.ts called it zero times before this fix |
| 954 | improvement | scripts/decision-log-append.ts | No improvement needed - code already clean | The touched lines (import, type pointer comment, validation call) already follow the file's established reject-loudly and pointer-comment conventions |
| 956 | root-cause | scripts/lib/check-common.ts, scripts/lib/check-common.test.ts, scripts/decision-log-append.ts | Guard placed inside renderIndexRowLine, not appendIndexRowIfAbsent, so one call covers both write branches plus doc-index-generate.ts's direct calls with zero extra touch-paths. | renderIndexRowLine is the single serialization point all three producers already funnel through; guarding the caller instead would require three separate call sites and risk missing one. |
| 956 | reuse | scripts/lib/check-common.ts, scripts/decision-log-append.ts | Reuse Check: reusing assertNoEmbeddedNewline (previously scripts/decision-log-append.ts, issue #940) — extracted to check-common.ts, 2 call sites. | check-common.ts now needs the identical guard for its own writer, making a second real production consumer; #940's single-consumer V-YAGNI-03 carve-out for keeping it local no longer holds. |
| 956 | improvement | scripts/lib/check-common.ts, scripts/decision-log-append.ts | No improvement needed — code already clean. | The diff is a minimal, plan-scoped extraction plus one new sibling detector function; no unrelated debt was found in the touched region to justify a Scout Check edit. |
| 957 | reuse | src/references/companion-file-sync.md | Reuse Check: none found — first occurrence of this concern (repo-wide); targeted prose correction, no new utility/abstraction introduced. | The change only corrects stale prose describing an existing repair function; nothing new was written to check for duplication against. |
| 957 | improvement | src/references/companion-file-sync.md | No improvement needed beyond the corrected V-ADA-09 section — the rest of the file (V-ADA-01, V-ADA-05, CLI invocation block, prefix lists) was independently re-verified against scripts/lib/companion-file-sync.ts and is confirmed current. | Scout Check requires either an in-scope improvement or an explicit 'already clean' finding; verification against live code showed no other stale content in the touched file. |
| 957 | approach | src/references/companion-file-sync.md | Documented the --upsert-journeys-index CLI flag's stale name (predates issue #832's behavior change) rather than renaming it; filed follow-up issue #955 for the rename. | Renaming the flag is a CLI surface change reaching scripts/lib/companion-file-sync.ts, src/SKILL.md and its 9 mirrors, templates/companion-files/README.md and its mirrors, and scripts/verify.cwd-pin-guard.test.ts — out of scope for this docs-only issue; a documented mismatch is more honest than silent renaming or silent omission. |
| 959 | reuse | scripts/checks/control-char.check.ts | Reuse Check: none found — first occurrence of concern (repo-wide). Reused check-utils.ts's root/CheckResult and test-fixtures.ts's withTempGitRepo/runGit; no existing module scans buffers for control characters, wraps git check-attr, or lists tracked files as a reusable helper. | Repo-wide grep for check-attr, control-char scanning, and a listTrackedFiles-shaped helper found zero prior implementations outside this diff, so building new is correct per V-INT-02 rather than duplicating a concern that already has a home. |
| 959 | improvement | scripts/checks/control-char.check.ts, scripts/lib/build/facts.ts | No improvement needed — code already clean. | control-char.check.ts is newly authored to the diff's own conventions; facts.ts received only a single-literal value bump (120 -> 121) at its grandfathered 300-LOC ceiling with zero headroom, so touching anything beyond that one value risked exceeding the ceiling. |
| 959 | root-cause | scripts/checks/control-char.check.ts, scripts/verify.control-char.test.ts | F-00135 fix: added a statSync size check (10MB default, DEFAULT_MAX_SCANNED_FILE_BYTES) before fs.readFileSync, skipping oversized tracked files with the same skip-and-continue semantics as unreadable paths. Kept the Threat Model row as 'Mitigated' (did not split into Accepted Risk) since the cap now makes that claim literally true. | Root cause was that the DoS mitigation comment described the try/catch as covering large files when it only covered unreadable paths (readFileSync succeeds and pulls arbitrarily large files fully into memory for anything under Node's ~2GB buffer limit, so it never throws for a large-but-plausible file). The alternative (splitting the row into Accepted Risk for large files) was considered and rejected: a 10MB cap is ~120x the largest currently tracked file, so the residual blind spot (a corrupted file padded past 10MB) is both implausible and self-defeating for an attacker, since a 10MB diff draws exactly the review scrutiny an invisible-binary diff does not — making 'Mitigated' the honest claim rather than a reworded gap. |
| 961 | root-cause | scripts/lib/worker-json/shared-validators.ts | Tightened validateDecisionRecord's pr/issue guard from at-least-one to hasPr === hasIssue | Error message already promised exactly-one; no confirmed caller relies on both-present, so tightening is zero-risk |
| 961 | reuse | scripts/lib/worker-json/shared-validators.ts | No shared exactly-one-of-N-fields helper existed to reuse; kept the local boolean rewrite | Repo-wide search found zero existing occurrences of this concern in scripts/lib/worker-json/ |
| 961 | improvement | scripts/lib/worker-json/shared-validators.ts | Named hasPr/hasIssue booleans instead of inlining isNumber() twice | Makes the exactly-one condition self-documenting at the call site |
