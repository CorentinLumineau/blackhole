{
  "version": 1,
  "description": "Optional Cursor SubagentStop hook — validates bc-campaign worker JSON against worker-schemas.md.",
  "hooks": {
    "subagentStop": [
      {
        "command": "bun run scripts/validate-worker-json.ts --hook",
        "matcher": "bc-planner|bc-implementer|bc-reviewer",
        "failClosed": true
      }
    ]
  },
  "_note": "Merge the hooks block into your project's .cursor/hooks.json (fragment, not a full hooks file). Requires bun on PATH; paths are relative to the repo root."
}
